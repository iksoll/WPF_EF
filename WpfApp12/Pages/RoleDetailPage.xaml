<Page x:Class="WpfApp12.Pages.RoleDetailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:material="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      Title="Пользователи роли"
      Background="{DynamicResource MaterialDesignPaper}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="Назад" Click="BackButton_Click" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,8,0"/>
            <TextBlock Text="{Binding Role.Title}" FontSize="20" VerticalAlignment="Center" FontWeight="Medium"/>
        </StackPanel>

        <ListView Grid.Row="1"
                  ItemsSource="{Binding Role.Users}"
                  SelectedItem="{Binding SelectedUser}"
                  SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Padding="12" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{Binding UserProfile.AvatarUrl}"
                                   Width="32" Height="32" Stretch="UniformToFill"
                                   Margin="0,0,8,0" Visibility="{Binding UserProfile.AvatarUrl, FallbackValue=Collapsed, TargetNullValue=Collapsed}"/>
                            <StackPanel>
                                <TextBlock Text="{Binding Login}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Name}" Foreground="{DynamicResource MaterialDesignBody}"/>
                                <TextBlock Text="{Binding Email}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>